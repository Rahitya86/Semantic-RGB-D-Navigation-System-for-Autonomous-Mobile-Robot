[metadata]
name = semantic_object_detection
version = 0.0.1

[develop]
script_dir=$base/lib/semantic_object_detection

[install]
install_scripts=$base/lib/semantic_object_detection

[options]
zip_safe = True
include_package_data = True
packages = find:

[options.packages.find]
where = .
